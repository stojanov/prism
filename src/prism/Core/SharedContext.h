#pragma once

#include "Pointers.h"
#include "SystemEventManager.h"
#include "SystemController.h"

namespace Prism::Core
{
	/*
	struct SharedContext
	{
		SystemEventManager SystemInput;
		SystemController SystemController;
		// Window
		// Asset Manager
		// Graphics/Renderer API
		// States/Layers/Component Renderer
	};

	using SharedContextRef = Ref<SharedContext>;

	inline SharedContextRef CreateSharedContext()
	{
		auto ctx = MakeRef<SharedContext>();

		return ctx;
	}
	*/
}
